part of 'pages.dart';

class TabletDashboardBody extends StatelessWidget {
  const TabletDashboardBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DesktopBody(
      isDesktop: false,
    );
  }
}
